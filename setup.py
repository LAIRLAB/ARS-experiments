from setuptools import setup

setup(name='ssil',
      version='0.0.1',
      install_requires=['gym'])

setup(name='sesil',
      version='0.0.1',
      install_requires=['gym'])

setup(name='imp_her',
      version='0.0.1',
      install_requires=['gym'])
